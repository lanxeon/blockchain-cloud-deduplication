import React from 'react';
import classes from './Main.module.css';

const Main = (props) => {
    return (
        <main className={classes.main}>
            <h1>Main body</h1>
        </main>
    );
}

export default Main;