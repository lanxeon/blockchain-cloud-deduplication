import React from 'react'

import UploadMenu from '../UploadMenu/UploadMenu';

const Main = (props) => {
    return (
        <div>
            <h1>Main body</h1>
        </div>
    );
}

export default Main;